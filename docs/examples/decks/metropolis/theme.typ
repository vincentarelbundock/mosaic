// The bundled Metropolis theme is a complete Mosaic facade. Re-export it as
// `m`; the constants below are deck-specific choices, not theme API.
#import "@local/mosaic:0.0.1" as mosaic
#import mosaic.themes.metropolis as m

#let ink = rgb("#23373b")
#let orange = rgb("#f28e2b")
#let soft = rgb("#dddddd")
#let mono = "Fira Mono"
