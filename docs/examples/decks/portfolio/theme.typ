// Exact Greyscale themed Mosaic facade vendored with this example deck.
#import "@local/mosaic:0.0.1": slide, note, fit, surface, grids, steps, components, themes
#import "theme-definition.typ": definition
#import "theme-layouts.typ" as layouts
#let setup = themes.setup(definition)
