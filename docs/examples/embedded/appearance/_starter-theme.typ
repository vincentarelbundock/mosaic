// Exact starter facade: portable Mosaic API plus its passive definition and layouts.
#import "@local/mosaic:0.0.1": slide, note, pause, surface, grid, steps, components, theme
#import "_starter-definition.typ": definition as _definition
#import "_starter-layouts.typ" as layouts
#let setup = theme.setup(_definition)
