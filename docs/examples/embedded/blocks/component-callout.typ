#import "@local/mosaic:0.0.1" as m

#show: m.setup

== `components.callout()`

#align(center + horizon)[
  #m.components.callout(
    [Null-only papers remain rare.],
    accent: rgb("#7c3aed"),
    title: [Main result],
  )
]
