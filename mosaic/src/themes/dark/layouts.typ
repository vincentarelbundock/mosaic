// Callable Dark layout namespace: the base layouts unchanged. Dark's design
// lives entirely in its colors and native rules.
#import "../../layout/api.typ": content, title, section, image
#import "../../author.typ": author
