// Canonical public Light facade; root Mosaic re-exports it exactly.
#import "../shared-api.typ": slide, note, pause, fit, surface, grid, steps, components, theme
#import "light/definition.typ": definition
#import "light/layouts.typ" as layouts
#let setup = theme.setup(definition)
