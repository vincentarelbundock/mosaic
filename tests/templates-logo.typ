#import "@local/mosaic:0.0.1" as mosaic
#show: mosaic.setup.with(features: (logo: [GLOBAL-LOGO]))
#mosaic.slide(grid: mosaic.templates.title([Title]))
#mosaic.slide(grid: mosaic.templates.default(variant: "body"))[Second]
